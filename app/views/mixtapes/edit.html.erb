<h3>Edit <%= @mixtape.name %></h3>

<%# Creator can edit %>
<% if current_user && current_user.id == @song.user_id %>
   <%= render partial: 'form' %>

<%# Edit only your content %>
<% elsif current_user  %>
   <p class="errors" id="error-details">You can only edit Mixtapes you have added.</p>
   <div class="actions-bot">
      <%# Navigation/Actions if logged in %>
      <h3>Actions</h3>
      <%= link_to "<< Back", back_path %>
      <%= link_to "Create a New Mixtape", new_mixtape_path, class: "new", id: "mixtape" %>
   </div>
<%# Nag for login %>
<% else %>
   <%= render partial: 'layouts/hey_dj' %> make edits. Sneaky, sneaky.</p>
<% end %>
